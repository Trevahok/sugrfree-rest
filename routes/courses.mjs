import ControllerSet from '../lib/controllerset.mjs'
import { loginRequiredMiddleware } from '../middlewares/auth.mjs'
import { RouterSet } from '../lib/routerset.mjs'


// const controllers =  new ControllerSet('course', courseSchema )
// const routers = new RouterSet(controllers, loginRequiredMiddleware)

export default routers.getRouter()